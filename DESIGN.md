# codemorph — デザインシステム

## Colors

| 名前 | HEX | 用途 |
| --- | --- | --- |
| Canvas | `#0B1116` | アプリ全体の背景 |
| Surface | `#121D23` | パネル、ヘッダー |
| Raised | `#1B2B31` | 入力欄、ポップオーバー、ホバー面 |
| Border | `#30464C` | パネルと操作部品の境界 |
| Text | `#EDF5F2` | 主な文字、選択中のラベル |
| Muted | `#A9BEC0` | 補助説明、メタデータ |
| Accent | `#83E3C6` | フォーカス、選択、主要操作 |
| Accent surface | `#25483F` | 選択したフィルターの背景 |
| Warning | `#F3B467` | TODO / FIXME と注意を要する印 |
| Map Japanese | `#69D8C3` | かなを含む日本語として解析した語の点 |
| Map Han | `#C7A6F4` | 漢字系の語。日本語か中国語かは未判定 |
| Map Latin / English | `#F3B467` | 英語またはラテン文字系の点 |
| Map Cyrillic | `#A5B5FF` | キリル文字系の点 |
| Map Arabic | `#E3A5EB` | アラビア文字系の点 |
| Map Devanagari | `#F2A6A2` | デーヴァナーガリー系の点 |
| Map Thai | `#9EC9EF` | タイ文字系の点 |
| Map Hangul | `#A9D88E` | ハングル系の点 |

その他の文字体系には安定した色を割り当てる。色だけで情報を伝えず、言語名・選択状態・数値も併記する。地図の語間距離は近似であり、色や距離を文章の評価として扱わない。

## Typography

本文は `Inter, "Hiragino Kaku Gothic ProN", "Noto Sans JP", system-ui, sans-serif`。コードとファイルパスは `ui-monospace, SFMono-Regular, monospace`。外部フォントは読み込まない。

| 役割 | サイズ | ウェイト | 行間 |
| --- | --- | --- | --- |
| 画面見出し | 28–36px | 700 | 1.15 |
| パネル見出し | 18–22px | 700 | 1.25 |
| 本文・操作 | 14px | 400 / 600 | 1.5 |
| 地図ラベル | 13–15px | 600 | 1.25 |
| 補助文 | 12px | 400 / 600 | 1.5 |
| 小見出し | 11px | 700 | 1.4 |

数値には等幅数字を使い、長い語やパスは折り返す。地図ラベルは衝突を避けて間引くが、語そのものは常に点として表示する。

## Spacing

ベースユニットは **4px**。余白スケールは `4 / 8 / 12 / 16 / 24 / 32 / 48px`。コンパクトなグラフ操作では 8px、パネル内では 16–24px、画面の主要領域間では 24px を基準にする。角丸は入力とボタンで 8px、パネルで 12px、ピル状フィルターで 999px。

## Components

- **グローバルヘッダー**: 高さ 64px。製品名、ローカル・読み取り専用の状態、日英切り替えを配置する。装飾だけのアイコンは使わない。
- **探索パネル**: デスクトップでは地図の左に幅 280–320px。検索、言語フィルター、件数、頻出語を上から順に配置する。狭い画面では地図の前に置く。
- **検索**: 高さ 44px。検索アイコンは入力の補助としてのみ使い、プレースホルダーとアクセシブルな名前は「単語を検索 / Search words」。結果は頻度順、クリックと Enter で地図の語を選択・中心へ移動する。
- **ボタン**: 主操作と文言付きの二次操作は高さ 40px 以上。アイコンだけのボタンは 40×40px 以上にし、`aria-label` とツールチップを持たせる。矢印だけで選択やリセットを表現しない。
- **言語フィルター**: 言語名、対応する点の色、語数を表示する。選択状態は塗りと枠線に加え `aria-pressed` で伝える。
- **地図**: 利用可能な領域を優先して広く使い、UMAP 座標の縦横比を保つ。点の大きさで頻度、明るさで TF-IDF、色で言語を示す。ズームしても点の画面上の大きさは一定にし、ラベルはズーム量と衝突に応じて増減する。ホバーとキーボードフォーカスで関係線と詳細を一時表示し、クリックまたは Enter で固定する。固定した点の再クリックで解除でき、周囲に大きな円を描かない。拡大・縮小・全体表示を文言または明確なアイコンで操作できる。
- **詳細パネル**: 一時表示中は操作を受け付けず、クリックで固定すると関連語と閉じるボタンを操作できる。固定中に別の語をホバーすると一時的に切り替わり、離すと固定した語に戻る。デスクトップでは点と反対側に地図を動かさず重ね、選択語、統計、近い語、別表記、共起、出現箇所の順に示す。モバイルでは閉じられる下部シートとする。
- **空状態**: 次の行動を文章と実行可能なコマンドで示す。装飾記号で操作を暗示しない。

## Elevation

| 深度 | シャドウ | 用途 |
| --- | --- | --- |
| 0 | なし | 地図、通常パネル |
| 1 | `0 8px 24px #0006` | 検索結果 |
| 2 | `0 20px 56px #0009` | 詳細パネル、空状態 |

深度は重なりを説明するために使う。通常の区切りにはシャドウではなく Border を使う。

## Guidelines

### Do's

- 探索の流れを「検索・絞り込み → 地図でプレビュー → クリックで固定」に揃える。
- 地図の点と語の対応を、ホバー・フォーカス・選択時にラベルと明瞭な輪郭で示す。
- 日英の UI 文言を同じ翻訳定義で管理する。内容の言語はモデル・解析手法が扱える範囲にする。
- 画面幅と高さに応じて地図を伸縮させ、タッチ操作とキーボード操作を可能にする。
- 近さの数値は元のベクトルに基づく値と明示し、出現箇所への導線を用意する。

### Don'ts

- `↖` や `↗` など意味が定まらない記号を操作説明やボタンの代わりにしない。
- 色や点の位置だけで意味、品質、優劣を断定しない。
- 未選択時に中身のない詳細パネルで地図の幅を削らない。
- 解析済みデータを外部に送る動線や、文章を採点する UI を設けない。
